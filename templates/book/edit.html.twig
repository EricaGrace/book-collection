{% extends 'base.html.twig' %}

{% block title %}Modifier le livre{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card shadow-sm bg-white">
            <div class="card-body">
                <h1 class="text-center mb-4">✏️ Modifier le livre</h1>

                {{ include('book/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

                <div class="d-flex justify-content-between mt-3">
                    <a href="{{ path('app_book_index') }}" class="btn btn-outline-secondary">← Retour</a>
                    {{ include('book/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
